name: Publish Documentation
on:
  push:
    branches:
      - master
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:


jobs:
  documentation:
      uses: sensirion/.github/.github/workflows/driver.python.documentation.yml@main
      with:
        run-environment: "ubuntu-24.04"
        build-python-version: 3.11
        use-project-requirements: true
